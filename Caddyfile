:80 {
    root * /usr/share/caddy
    encode gzip zstd
    file_server
    header {
        X-Content-Type-Options "nosniff"
        X-Frame-Options "SAMEORIGIN"
    }
    log {
        output stdout
        format console
    }
}
